/**
 * The color associated with primary elements or user flows.
 */
const primary = "cornflowerblue"

/**
 * The color associated with destructive actions.
 */
const danger = "lightsalmon"

export default { primary, danger }
